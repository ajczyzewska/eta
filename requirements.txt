# EPUB to Audiobook - Dependencies
# Generowanie audiobooka z plików EPUB

# TTS (Text-to-Speech)
# Note: Requires Python 3.9-3.11 (not compatible with Python 3.12+)
TTS==0.22.0

# EPUB processing
ebooklib>=0.18
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Audio processing
pydub>=0.25.1

# CLI and progress bars
rich>=13.0.0

# System monitoring (for optimization)
psutil>=5.9.0

# PyTorch (for TTS model)
# Using specific versions for compatibility with TTS 0.22.0
torch==2.1.0
torchaudio==2.1.0

# Transformers (required by TTS)
transformers==4.33.0

# Web UI (optional)
Flask>=2.2.0
Werkzeug>=2.2.0
